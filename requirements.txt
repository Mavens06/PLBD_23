# Dépendances Python pour le projet Agri-Botics
# Installer avec : pip install -r requirements.txt

# --- Machine Learning ---
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# --- Téléchargement de données ---
requests>=2.31.0

# --- Backend API (FastAPI) ---
fastapi>=0.115.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# --- LLM Chatbot (Gemini via Google AI Studio) ---
# Le chatbot appelle l'API Gemini (cloud) en REST via httpx. Aucune lib SDK
# spécifique requise. L'inférence ML/règles reste 100% locale ; seul le chat
# utilise le cloud (clé GEMINI_API_KEY + connexion internet).
# Clé gratuite : https://aistudio.google.com/apikey
httpx>=0.27.0

# --- Communication série (capteur 4-en-1 RS485 sur Raspberry Pi) ---
# pyserial est utilisé par minimalmodbus. minimalmodbus est nécessaire
# uniquement sur le robot réel ; le mock sensor n'en a pas besoin.
pyserial>=3.5
minimalmodbus>=2.1.0 ; platform_machine == "armv7l" or platform_machine == "aarch64"

# --- Utilitaires ---
python-dotenv>=1.0.0
